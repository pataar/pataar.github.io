pre-commit:
  commands:
    format:
      glob: "*.{cjs,js,json,jsonc,md,mjs,ts,yaml,yml}"
      run: bun oxfmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{astro,cjs,js,mjs,ts}"
      run: bun oxlint --fix {staged_files}
      stage_fixed: true
