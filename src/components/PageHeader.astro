---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<header>
	<h1 class="text-3xl font-bold text-fg"><span aria-hidden="true" class="select-none text-comment"># </span>{title}</h1>
	<p class="mt-4 leading-relaxed text-fg-muted">
		<slot />
	</p>
</header>
